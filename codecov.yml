coverage:
  status:
    project:
      default: off
      common:
        flags:
          - common
      restapi:
        flags:
          - restapi

flags:
  common:
    paths:
      - packages/core/
  restapi:
    paths:
      - packages/restapi/
