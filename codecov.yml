coverage:
  status:
    project:
      default: off
      core:
        flags:
          - core
      restapi:
        flags:
          - restapi

flags:
  core:
    paths:
      - packages/core/
  restapi:
    paths:
      - packages/restapi/
